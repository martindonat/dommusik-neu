const r = (n, t) => {
  const e = new URL(t);
  return e.hostname = `${n}.${e.hostname}`, e.toString().replace(/\/$/, "");
};
const o = (n) => n.toString(36).slice(0, 10), a = (n, t) => {
  try {
    const e = new URL(n).hostname, s = new URL(t).hostname;
    return e === s || e.endsWith(`.${s}`);
  } catch {
    return !1;
  }
};
export {
  r as a,
  o as c,
  a as i
};
